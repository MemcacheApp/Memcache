export enum Experience {
    Beginner,
    Intermediate,
    Advanced,
}

export enum Finetuning {
    Qualitative,
    Quantitative,
    Mixed,
}

// For flashcard generation
export enum Range {
    Depth,
    Breadth,
    Balanced,
}
